<%= render 'layouts/errors', object: @lesson  %>



<%= bootstrap_form_for(@lesson) do |f|  %>
  <%= f.text_field :title %>
  <%= f.text_area :content %>
  <%= f.number_field :lesson_number %>
  <%= f.submit data: { disable_with: "Submitting..."}, class: "btn btn-success btn-block" %>
<% end  %>
