<%= content_for(:title, "Lessons | LHTP") %>

<% if @lessons.any? %>
  <h3>Here are the lessons:</h3>
  <ul>
    <% @lessons.each do |lesson| %>
      <li>
        <%= link_to lesson.title, lesson_path(lesson) %>
      </li>
    <% end %>
  </ul>
<% else %>
  <h3>There are no lessons yet :(</h3>
<% end %>
